#include <iostream>
#include <bits/stdc++.h>
#include <algorithm>

using namespace std;

int main()
{
    int t; cin>>t;
    while(t--)
    {
        int n,k;
        int A[n];
        sort(A,A+n);
        for(int i=0;i<n;i++){
            if(A[i]==A[i+1])
            {
                int count=0;
                count++;
                
            }
        }
        int V[n];
        for(int i=0;i<=count;i++)
        {
            V[i]+=A[i];
        }
        for(int i=0;i<sizeof(V);i++)
        {
            cout<<V[i]<<" "<<endl;
        }
    }
    
}
